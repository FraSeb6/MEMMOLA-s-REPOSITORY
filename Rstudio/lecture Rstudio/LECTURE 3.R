set.seed(42)# set the random seed is for not genarete the random generagion 
sample(x =c("HEAD", "TAIL"),size =10, replace=TRUE)



set.seed(33) # With for loops
res <- vector(mode = "list",length = 4)
for (i in 1:4) {
  res[[i]] <- runif(n = i,min = 0,max = 10)
}
res


#computatilly speaking: belive this one is faster than the for loop, but surley is way shorter
set.seed(33) # With lapply using the dot-dot-dot argument
(res <- lapply(X = 1:4, FUN = runif, min=0,max=10))



set.seed(33) # With lapply explicitly defining FUN
(res <- lapply(
  X = 1:4,
  FUN = function(num)
    runif(n = num, min = 0, max = 10)
))



x <- list(a = 1:10, b = 1:100)

sapply(x, FUN = mean)
#ex in sapply stating an element simplify=FALSE,will return the same list that has been generated by lapply()























































































































































































